# 可靠性

Verba 的可靠性目标是：即使音频、网络、AI 服务或 SSE 推送局部失败，用户仍然能理解当前状态，并尽量保留已有字幕结果。

## 基本原则

- 重连或重试时保留已有字幕，不要清空用户上下文。
- 监听中、重连中、已停止、API 错误、静音输入等状态要明确。
- 修正必须依赖稳定的 `segmentId` 和递增的 `revision`，保证幂等。
- 外部服务调用必须有超时、重试和降级策略，不能无限阻塞 UI。

## 当前重点

- 会话生命周期正确性。
- SSE 推送和重连行为。
- 音频 chunk 校验和缓冲。
- ASR、翻译、修正、TTS 的超时处理。
